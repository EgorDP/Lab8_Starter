1. How do I make functions outside of test(...) / it(...) statements that can use puppeteer commands?

2. Why does iterating through prodItems work with different starting indices: 0,1 for clicking and for reading text, respectively? 